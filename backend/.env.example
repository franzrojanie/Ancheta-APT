# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MySQL / MariaDB)
# Development (XAMPP/local): localhost, port 3307
# Production (cPanel): localhost, port 3306
# Production (Remote MySQL): your-host.com
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ancheta_apartment
DB_USER=root
DB_PASSWORD=

# JWT Configuration
# Generate a secure key: openssl rand -base64 32
# IMPORTANT: Change this in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
# Development
FRONTEND_URL=http://localhost:5173

# Production on Render
# FRONTEND_URL=https://ancheta-apartment-frontend.onrender.com

# Production on cPanel
# FRONTEND_URL=https://yourdomain.com

# PayMongo Configuration (optional)
# Get your secret key from PayMongo dashboard at https://dashboard.paymongo.com
# For development, you can use the test key below
# For production, use your live key (sk_live_xxx)
# Leave empty if not using PayMongo
PAYMONGO_SECRET_KEY=sk_test_vdnnC2dZRARq96ujUEodXaNq
